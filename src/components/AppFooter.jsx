import React from 'react';

import { Footer } from '@mantine/core';

const AppHeader = () => {
  return (
    <Footer sx={{ position: 'sticky', top: '100vh' }} height={100} withBorder>
      Footer
    </Footer>
  );
};

export default AppHeader;
